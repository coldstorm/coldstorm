Services.factory("Input", ["$rootScope", "Connection", function ($rootScope, Connection)
{
    $rootScope.process = function (input)
    {
        //Parse commands

        //Parse formatting

        //Send the line

        //Clear the input

        //Add the line to the current tab
    }
}]);